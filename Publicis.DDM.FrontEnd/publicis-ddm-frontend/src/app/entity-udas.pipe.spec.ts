import { EntityUserDefinedAttributesPipe } from './entity-udas.pipe';

describe('EntityUDASPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityUserDefinedAttributesPipe();
    expect(pipe).toBeTruthy();
  });
});
