import { EntityValuesPipe } from './entity-values.pipe';

describe('EntityValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
